# 温度获取问题 - 快速诊断指南

## 问题现象
- ✓ nRF Mesh App 能获取温度
- ✗ 你的 App 获取不到温度

## 已完成的改进

### 1. 改进了 `readTemperature()` 函数
添加了详细的诊断日志和错误检查：
- 检查 BLE 连接状态
- 检查 Mesh 网络状态
- 检查 App Key 是否存在
- 查找设备节点
- 查找 Sensor Server 模型 (0x1100)
- 检查模型的 App Key 绑定状态
- 发送到正确的元素地址

### 2. 改进了消息接收日志
在 `onUnknownPduReceived` 中添加了 opcode 日志

### 3. 添加了调试函数
`debugNetworkConfig()` - 打印完整的网络配置信息

### 4. 创建了诊断脚本
`diagnose_temperature.sh` - 自动监控和分析日志

## 诊断步骤

### 步骤 1: 查看网络配置
在你的 App 中添加一个按钮或在启动时调用：
```kotlin
viewModel.debugNetworkConfig()
```

查看日志输出，确认：
- [ ] Sensor Server 模型存在
- [ ] Sensor Server 绑定了 App Key
- [ ] 设备地址正确

### 步骤 2: 测试温度读取
1. 确保设备已连接
2. 点击读取温度按钮
3. 查看日志输出

**期望看到的日志：**
```
=== 读取温度 ===
节点地址: 0x0002
目标地址: 0x0002
App Key Index: 0
Property ID: null (获取所有传感器)
Sensor Get 已发送
```

### 步骤 3: 使用诊断脚本
```bash
cd /home/meng/code/android/BLE_Device_Mesh
./diagnose_temperature.sh
```

然后在 App 中点击读取温度，脚本会自动分析日志。

### 步骤 4: 对比固件日志
同时查看固件的串口输出，应该看到：
```
=== Sensor Get ===
From: 0x0001, Property: 0x0000, Buf len: 0
Send sensor status: temp=0x32
```

## 可能的问题和解决方案

### 问题 1: 日志显示 "未找到 App Key"
**原因**: 网络配置有问题

**解决方法**:
1. 重新导入网络配置
2. 或者重新配网设备

### 问题 2: 日志显示 "设备没有 Sensor Server 模型"
**原因**: 
- 设备固件没有正确注册 Sensor Server
- 或者配网时没有正确识别模型

**解决方法**:
1. 检查固件的模型注册代码
2. 使用 nRF Mesh App 查看设备的模型列表
3. 如果模型存在但 App 找不到，可能是配网数据不同步

### 问题 3: 日志显示 "Sensor Server 未绑定 App Key"
**原因**: 模型没有绑定 App Key，无法接收消息

**解决方法**:
使用 nRF Mesh App 绑定 App Key：
1. 打开 nRF Mesh App
2. 选择设备
3. 找到 Sensor Server 模型
4. 点击 "Bind App Key"
5. 选择 App Key 0

### 问题 4: App 发送了消息，但固件没有收到
**原因**: 
- 消息发送到了错误的地址
- 加密/解密失败
- 网络配置不一致

**解决方法**:
1. 确认 App 和 nRF Mesh App 使用相同的网络配置
2. 检查 App Key 是否一致
3. 尝试重新配网设备

### 问题 5: 固件收到消息并回复，但 App 收不到
**原因**: 
- App 没有正确处理 SensorStatus 消息
- 消息解析失败

**解决方法**:
查看日志中是否有：
- "收到 SensorStatus" - 说明收到了消息
- "收到未知 PDU" - 说明消息被识别为未知类型
- "解析到温度" - 说明解析成功

如果看到 "收到未知 PDU"，检查 opcode 是否为 0x52。

## 对比测试

### 使用 nRF Mesh App
1. 打开 nRF Mesh App
2. 连接到设备
3. 选择 Sensor Server 模型
4. 点击 "GET"
5. 查看固件串口日志

### 使用你的 App
1. 打开你的 App
2. 连接到设备
3. 点击读取温度
4. 查看 App 日志和固件串口日志

### 对比分析
| 项目 | nRF Mesh App | 你的 App | 是否一致 |
|------|--------------|----------|----------|
| 固件收到消息 | ✓ | ? | ? |
| 固件发送响应 | ✓ | ? | ? |
| App 收到响应 | ✓ | ? | ? |
| App 解析成功 | ✓ | ? | ? |

## 最可能的原因（按概率排序）

1. **Sensor Server 未绑定 App Key** (80%)
   - 症状: 固件没有收到消息
   - 解决: 使用 nRF Mesh App 绑定 App Key

2. **发送到错误的地址** (60%)
   - 症状: 固件没有收到消息
   - 解决: 改进后的代码会自动查找正确的元素地址

3. **网络配置不一致** (40%)
   - 症状: 加密/解密失败
   - 解决: 确保使用相同的网络配置文件

4. **发送时机不对** (20%)
   - 症状: 偶尔能收到，但不稳定
   - 解决: 确保在连接稳定后发送

5. **消息解析错误** (10%)
   - 症状: 固件发送了响应，但 App 没有显示
   - 解决: 检查解析代码（已经是正确的）

## 下一步行动

1. **重新编译并安装 App**
   ```bash
   cd /home/meng/code/android/BLE_Device_Mesh
   ./gradlew installDebug
   ```

2. **运行诊断脚本**
   ```bash
   ./diagnose_temperature.sh
   ```

3. **在 App 中调用调试函数**
   在 MainActivity 或 DeviceDetailActivity 中添加：
   ```kotlin
   // 在连接成功后调用
   viewModel.debugNetworkConfig()
   ```

4. **测试温度读取**
   点击读取温度按钮，观察日志

5. **对比固件日志**
   同时查看串口输出

6. **根据日志结果调整**
   - 如果固件没收到消息 → 检查模型绑定
   - 如果固件收到但 App 没收到响应 → 检查消息接收代码
   - 如果都正常但没有显示 → 检查 UI 更新代码

## 需要提供的信息（如果问题仍然存在）

1. **App 日志**（从点击读取温度到超时的完整日志）
2. **固件串口日志**（同一时间段）
3. **debugNetworkConfig() 的输出**
4. **nRF Mesh App 的设备信息截图**（显示模型和绑定状态）

## 联系方式

如果问题仍然存在，请提供上述信息，我会进一步分析。
