## 蓝牙 Mesh 控制 App UI 文字描述

## 图片位置

位于 images/

- `app图标.png` — 应用图标源图
- `首页横幅.png` — 首页顶部横幅
- `灯图标.png` — 设备卡片图标

---

### 1. 总体布局

- **页面类型**：移动端主界面 + 设备详情界面 + 分组管理 + 配网 + 设置
- **风格**：Material Design 3，暖橙主题，圆角卡片
- **背景颜色**：`#FAF5F0`（暖米白）
- **主色**：`#FF6B35`（暖橙）
- **滚动方向**：垂直滚动（设备列表可上下滑动）
- **主要分区**：
  1. 顶部设备 Mesh 信息区域
  2. 首页横幅（自定义图片）
  3. 场景选择模块（横向滚动）
  4. 设备卡片列表（2 列网格）
  5. 底部导航栏（3 标签）

---

### 2. 顶部 Mesh 信息区域

- **左上角**：标题 "我的家"，24sp 加粗
- **次级信息**：蓝牙 Mesh 网络状态，绿色圆点 + "Mesh 在线"
- **右上角**：设置按钮（⚙）+ 配网设备按钮（+）
- **本机地址**：底部显示当前 Provisioner 地址

---

### 3. 首页横幅

- **卡片式横向矩形**，120dp 高度
- 使用 `images/首页横幅.png` 原始图片
- `scaleType="centerCrop"`，带阴影

---

### 4. 场景选择模块

- **排列**：横向水平滚动
- 每个场景：72x80dp 白底圆角卡片（elevation=1dp）
- 图标 + 文本描述：
  1. 回家模式（🏠）+ 橘色背景图标
  2. 观影模式（🎬）+ 浅蓝背景图标
  3. 阅读模式（📚）+ 浅绿背景图标
  4. 睡眠模式（🌙）+ 浅紫背景图标

---

### 5. 设备卡片列表

一行 2 个卡片（`GridLayoutManager` span=2）

- **卡片样式**
  - 圆角 10dp CardView
  - 白色背景，2dp 阴影
  - 边距 4dp，内边距 10dp
  - 点击可打开设备详情

- **每个设备卡片包含（从上到下）**：
  1. **第一行**（左右布局）：
     - 左侧：在线状态（绿色圆点 6dp + "在线" 文字 10sp），设备在线时显示
     - 左侧下方：分组名称（如 "卧室"），12sp 灰色
     - 右侧：设备图标（40dp 圆形容器 + 28dp 自定义图片 `ic_device_light.png`）+ 设备名称（14sp 加粗）
  2. **亮度控制条**：SeekBar + 百分比数字
  3. **底部操作按钮**：灰色背景条，左右各一个：
     - ⏱ 定时 — 进入定时任务页面
     - 🔗 分组 — 管理分组

- **已移除**：设备地址、光照度、温度显示
- **已移除**：长按编辑模式、删除按钮、拖拽排序

---

### 6. 设备详情界面

- **顶部**：暖橙色调工具栏
- **设备信息卡片**
  - 大型设备图标
  - 名称 + 位置 + 在线状态 + 地址
- **核心控制区**
  - 开关按钮（橘色开启 / 灰色关闭）
  - 亮度滑条 + 百分比
  - 色温控制（冷/暖）
  - 光照度显示（lux）
  - 时间同步
  - 定时任务设置
  - 中继设置
  - 路灯模式
  - 重新绑定模型
  - 节点重置

---

### 7. 分组管理页面

- 暖橙色调工具栏 + "← 分组管理"
- 分组 CardView 列表
  - 左侧橙色装饰条
  - 在线状态指示点
  - 分组名称 + 地址 + 成员数
  - 编辑按钮（✏️）
  - 开关按钮
  - 亮度滑条
  - 设备在线摘要
- 底部 "+ 创建新分组" 按钮
- 空状态："暂无分组"

---

### 8. 底部导航栏

- 3 个标签，60dp 高度，白色背景
- 每个标签：图标（20sp）+ 文字（11sp）
- 选中态：文字加粗 + 橘色（`#FF6B35`）
- 未选中态：灰色（`#9CA3AF`）

1. 🏠 **首页**（默认选中）
2. 📁 **分组**
3. 👤 **我的**（设置页面）

---

### 9. 应用图标

- **格式**：自适应图标（Android 8+）
- **背景**：纯色 `#FF6B35` 橘色
- **前景**：`ic_app_foreground.png`（从 `images/app图标.png` 缩放到 216x216）
- **各密度**：48/72/96/144/192px PNG

---

### 10. 颜色规范

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 | `#FF6B35` | 按钮、选中态、装饰条 |
| 主色暗 | `#E55A2B` | 按压态 |
| 背景 | `#FAF5F0` | 页面背景 |
| 表面 | `#FFFFFF` | 卡片、弹窗 |
| 在线 | `#4CAF50` | 在线状态点 |
| 离线 | `#BDBDBD` | 灰色圆点 |
| 文本主 | `#2D2D2D` | 标题、正文 |
| 文本次 | `#9CA3AF` | 辅助信息 |
| 文本浅 | `#6B7280` | 分组名等 |
| Tab 未选中 | `#9CA3AF` | 底部导航未选中 |
| 亮度数值 | `#FFB300` | 暖黄高亮 |
| 删除/危险 | `#D32F2F` | 删除操作 |

---

### 11. 风格与交互

- **卡片圆角**：10-14dp
- **按钮**：圆角大按钮，触控区域大
- **滑条**：系统原生 SeekBar，橘色进度
- **整体风格**：Material Design 3，暖橙色调，柔和舒适
- **已移除功能**：长按编辑/拖拽排序/删除卡片
